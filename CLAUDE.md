# Global Claude Code Rules

## Java/Spring Boot 프로젝트 보조 지침

현재 작업 중인 프로젝트가 Java/Spring Boot(Gradle/Maven)로 판단되면 `~/.claude/CLAUDE_spring.md`를
먼저 읽고 그 안내(재사용 가능한 spring-* 에이전트/스킬 하네스 목록과 사용법)를 따른다.

## Next.js/React 프로젝트 보조 지침

현재 작업 중인 프로젝트가 Next.js/React(`package.json`에 `next` 의존성)로 판단되면
`~/.claude/CLAUDE_react.md`를 먼저 읽고 그 안내(재사용 가능한 react-* 에이전트/스킬 하네스 목록과
사용법, 프로젝트 성향 결정, 신규 프로젝트 스캐폴딩 순서)를 따른다.

## PDCA Document Naming Convention

PDCA 문서 작성 시 파일명 앞에 순번을 붙인다. 순번은 `.pdca-status.json`의 feature 등록 순서 기준으로 부여한다.

### Format
```
{NN}-{feature-name}.{type}.md
```

- `NN`: 2자리 순번 (01, 02, 03, ...)
- `feature-name`: 피처 이름 (kebab-case)
- `type`: plan, design, analysis, report

### Examples
```
docs/01-plan/features/01-game-blog-automation.plan.md
docs/02-design/features/01-game-blog-automation.design.md
docs/03-analysis/01-game-blog-automation.analysis.md
docs/04-report/features/01-game-blog-automation.report.md
```

### Archive Folder Format
```
docs/archive/YYYY-MM/{NN}-{feature-name}/
```

아카이브 폴더명에도 동일한 순번을 포함한다.

```
docs/archive/2026-02/18-pipeline-wizard-ui/
docs/archive/2026-02/35-pipeline-ux-fix/
```

### Rules
- 새 PDCA feature 시작 시 기존 문서 중 가장 큰 번호 + 1을 부여
- 같은 feature의 plan/design/analysis/report/archive 폴더는 모두 동일한 번호 사용
- 번호는 작업 순서를 나타내므로 변경하지 않음

## 코드 변경 이력 주석 (Plan Traceability)

PDCA Plan에 의해 코드가 추가·변경될 때, 해당 코드 블록에 어떤 Plan의 요청으로 처리되었는지 주석으로 명시한다. 이후 다른 Plan에 의해 같은 코드가 변경되면, 변경 이력을 누적하여 기록한다.

### 규칙
- 최초 생성 시: `// [#NN feature-name] 생성` 형태로 주석 추가
- 이후 변경 시: 기존 주석 아래에 `// [#NN feature-name] 변경내용` 추가
- 주석 위치: 변경된 코드 블록의 바로 위 (함수, 상수, JSX 블록 등 의미 단위)
- 사소한 변경(오타 수정 등)은 생략 가능
- `NN`은 `.pdca-status.json` 기준 피처 순번

### Examples
```typescript
// [#01 game-blog-automation] 생성
// [#05 streaming-display-fix] 스트리밍 로직 변경
const streamHandler = async () => {
  // ...
};
```

```tsx
// [#07 blog-management] 생성
// [#14 trend-search-expand] 검색 기능 추가
// [#15 rss-title-and-donga-fix] 타이틀 "게임 뉴스" → "RSS" 변경
<h2>RSS</h2>
```

```typescript
// [#01 game-blog-automation] 생성
// [#15 rss-title-and-donga-fix] 게임동아 URL 끝에 "/" 추가
const RSS_FEEDS = {
  // ...
};
```

## 파일 삭제 안전 규칙

파일을 삭제하거나 `rm -rf`를 실행하기 전에 반드시 사용자에게 구체적 범위를 확인한다:

- 삭제 대상 파일/폴더 목록을 먼저 보여주고 확인받기
- "전체 삭제" 같은 모호한 선택지 대신 단계별로 확인 (예: "활성 폴더 삭제" → "archive 폴더도 삭제?")
- git에 커밋되지 않은 파일 삭제 시 "복원 불가" 경고 필수
- 한 번에 여러 폴더를 삭제하지 말고, 각 단계마다 확인

## 한글 사용 규칙

모든 출력물에서 한글을 사용한다:

- 코드 주석은 한글로 작성
- PDCA 문서 (plan, design, analysis, report) 본문은 한글로 작성
- 갭 분석 결과 및 보고서는 한글로 작성
- 사용자에게 보여주는 설명은 한글로 작성
- 변수명, 함수명, 파일명 등 코드 식별자는 영문 유지

## 계획 변경은 append-only

실행 중 계획(Plan/Task 목록)을 바꿔야 할 일이 생기면, 기존 항목을 조용히 고치거나 지우지 않고
새 항목을 추가하는 방식으로 반영한다. 원본 계획은 그대로 감사(audit) 흔적으로 남긴다.

- 파생된 보정 작업(fix task)이 또 다른 보정 작업을 낳지 않도록 한다 — 파생은 1단계까지만.
  같은 지점에서 또 파생이 필요해지면 계속 이어가지 말고 사용자에게 알린다.
- 요구사항/목표처럼 상위 결정 사항은 실행 중 함부로 재검토하지 않는다("고정"). 바뀌는 건
  주로 태스크 단위의 세부 구현이다("가변"). 무엇이 고정이고 무엇이 가변인지 헷갈리면
  먼저 사용자에게 확인한다.

## 반복 실패 시 서킷 브레이커

같은 원인으로 보이는 실패가 반복되면(대략 3회 내외), 같은 방식으로 계속 재시도하지 않는다.
멈추고 지금까지 시도한 것·실패 원인·남은 선택지를 정리해 사용자에게 보고한 뒤 다음 지시를 받는다.

## 완료 보고에는 증거를 붙인다

"컴파일된다/타입체크 통과한다"와 "실제로 작동한다"는 다른 것이다. 화면(UI)·API 응답처럼
눈에 보이는 결과물이 걸린 작업은, 코드 변경만 보고 완료로 보고하지 않는다.

- 템플릿·CSS·이미지·프론트엔드 변경: 가능하면 로컬에서 실행/렌더링해서 실제 결과(curl 응답,
  화면 확인, 스크린샷 등)를 확인한 뒤에만 "완료"라고 보고한다. 확인이 불가능한 상황이면
  "코드는 변경했지만 실행 확인은 못 했다"고 명시적으로 밝힌다.
- 검증 없이 "완료"라고 말해놓고 나중에 실제로는 비어있거나 깨져있는 것이 가장 나쁜 실패다 —
  구조적으로 맞다는 것과 사용자가 보기에 정상이라는 것은 별개다.

## 스스로 세운 계획은 실행 전 재검증

여러 단계로 나뉜 계획을 스스로 수립했다면, 실행을 시작하기 전에 그 계획이 사용자의 원래
요청 범위를 실제로 다 커버하는지 다시 대조한다. 계획의 일부만 실행하고 계획대로 됐다는
이유로 "완료"라고 선언하지 않는다 — 계획 자체가 축소돼 있었을 가능성을 항상 의심한다.

## 체크 실패 시엔 수정이 먼저, 심화 검토는 그다음

lint·타입체크·테스트·빌드처럼 자동으로 확인 가능한 오류가 있으면, 그것부터 다 해결한 뒤에
구조 개선이나 스타일 다듬기 같은 심화 작업에 시간을 쓴다. 기본이 깨진 상태에서 다듬어봤자
다음 수정 때 다시 갈아엎히기 쉽다.

## 잘못된 접근은 되돌리고 다시 시작

구현 중간에 접근 자체가 틀렸다고 판단되면, 잘못된 코드 위에 계속 패치를 쌓지 않는다.
`git diff`/`git status`로 지금까지 뭐가 바뀌었는지 먼저 확인한 뒤, 문제 있는 부분만 깨끗하게
되돌리고 나서 다시 구현한다. 되돌리기와 재구현을 한 번에 뒤섞지 않는다.

## 신규 프로젝트는 하네스(Agent Teams) 먼저 켜고 시작

새 프로젝트를 시작할 때는 작업에 들어가기 전에 Agent Teams(`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`)가
켜져 있는지 먼저 확인한다. 꺼져 있으면 켜고(설정 반영에 세션 재시작이 필요하면 그렇게 안내), 그
다음에 실제 작업(PDCA 등)을 시작한다.

- Starter 레벨은 Team Mode를 지원하지 않으므로 (bkit 기준) 단독 세션으로 진행한다.
- "새 프로젝트"란 이번 대화에서 프로젝트 스캐폴딩부터 새로 시작하는 경우를 말한다. 이미 진행 중인
  기존 프로젝트에 이어서 작업하는 경우는 매번 다시 확인하지 않는다.

### feature마다 혼자/팀 모드는 매번 자율 판단

하네스를 켜두는 것과, 개별 feature를 solo로 할지 `cto-lead` 팀(Dynamic/Enterprise: developer/frontend/qa
등) 모드로 할지는 별개 결정이다. 매번 사용자에게 묻지 않고 아래 기준으로 스스로 판단하되, 어떤 모드를
왜 골랐는지는 한 줄로 알린다(조용히 바꾸지 않는다).

- 독립적으로 병렬화할 트랙이 실제로 있는가(예: 백엔드 API와 프론트 UI를 동시에 진행 가능, 여러
  feature를 동시에 굴려야 함) → 팀 모드
- 하나의 응집된 작업 단위(예: 도메인 모델 + 보안 설정처럼 순차적으로만 의미 있는 백엔드 feature)라
  나눌 이득이 적다 → 혼자
- 판단이 애매하면 혼자 쪽으로 기운다 — 팀 모드는 조율 비용이 들고, 여러 서브에이전트가 프로젝트
  고유 규칙(PDCA 번호, 한글 주석, 코드 이력 주석 등)을 일관되게 지키기 더 어렵기 때문.

## 신규 프로젝트는 오케스트레이션 체계를 선택해 프로젝트 CLAUDE.md에 고정한다

bkit PDCA와 개인 하네스(예: Java/Spring이면 `~/.claude/CLAUDE_spring.md`의 spring-webapp)처럼
같은 프로젝트에 적용 가능한 오케스트레이션 체계가 둘 이상이면, 새 프로젝트 시작 시 AskUserQuestion으로
어느 쪽을 쓸지 먼저 확인한다. 실제 작업(스캐폴딩 등) 전에 정한다.

- 사용자가 고른 체계를 프로젝트 루트 `CLAUDE.md`(project-local, 저장소에 커밋되는 파일)에
  `## 오케스트레이션 체계` 같은 명확한 제목으로 기록한다 — 어떤 체계인지, 선택한 날짜.
- 다음 세션부터는 프로젝트 CLAUDE.md에 이미 기록이 있으면 **다시 묻지 않고 그대로 따른다** — 이게
  이 규칙의 목적이다: 매번 들어갈 때마다 두 체계가 섞여서 혼선이 나는 걸 막는 것.
- 기록이 없는 기존 프로젝트(이 규칙 생기기 전에 시작한 프로젝트 등)를 만나면, 프로젝트 상태로
  이미 쓰고 있는 체계를 추정할 수 있으면(예: `docs/01-plan/`이 있으면 bkit, `_workspace/`가
  있으면 개인 하네스) 그걸로 기록해두고 알리기만 한다. 추정이 애매하면 그때 한 번 물어본다.
- 한번 기록된 선택은 대화 중 스스로 뒤집지 않는다 — 사용자가 명시적으로 바꿔달라고 해야 변경한다
  (append-only 원칙과 같은 맥락).

## 검증 강도는 작업 규모에 맞춘다

완료 증거 확인, 계획 재검증, 서킷 브레이커 같은 규칙은 복잡하거나 여러 시스템이 얽힌 작업,
같은 실수가 반복되는 상황에서 특히 힘을 준다. 간단하고 범위가 명확한 작업까지 매번 무겁게
검증하려다 오히려 속도만 떨어뜨리지 않도록, 작업 난이도에 맞춰 강도를 조절한다.
